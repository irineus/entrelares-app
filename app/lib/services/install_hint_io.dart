import 'package:entrelares_core/entrelares_core.dart';

/// Native half: an app that is installed has nothing to install. The question
/// only exists in a browser, so there are no facts to read here.
BrowserInstallFacts? readBrowserInstallFacts() => null;
